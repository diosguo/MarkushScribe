# M1 实施方案：显式 NODE AR baseline

> 状态：**已实现（v1，2026-09）**。T1–T7 全部落地，154 tests / ruff 通过；
> 单 batch 过拟合与 `--debug` CPU 复现已验证（见文末「实施记录」）。
> 依赖：`方案.md`（总体设计，尤其 §5.2/§6/§7/§9/§11–§18/§20）、`docs/m0_report.md`（M0 已验收）。
> 兄弟仓库：`../MarkushRender`（渲染与 `markush_graph_v2` 真值，只读使用，不修改）。

---

## 0. 背景与现状（M0 已完成）

已可用（MarkushScribe）：

| 模块 | 作用 |
| --- | --- |
| `markushscribe/schema.py` | `markush_graph_v2` dataclass + `parse_target`/`validate_target`/`to_dict` |
| `markushscribe/markushgen/` | SMILES → Markush IR（`abstract_molecule`、CLI） |
| `markushscribe/split.py` | canonical/scaffold 分组切分 + 泄漏检查 |
| `markushscribe/transforms.py` | 像素退化 + 几何 warp（`warp_target`）+ `ink_fraction` |
| `markushscribe/dataset.py` | `render_sample`/`build_dataset`/`validate_dataset`/`read_shard`/`RenderConfig`/`ShardStats` |
| `markushscribe/visualization/overlay.py` | `draw_overlay`（anchor/节点/边/注释回画） |
| `tools/*.py` | `fetch_chembl_sample` / `build_render_dataset` / `validate_dataset` / `inspect_dataset` / `check_split_leakage` |

数据：`data/rendered/{train,valid,test}/shard_*.tar`，每个 tar 含 `<uid>.png` + `<uid>.json`（v2 target）。
MarkushRender 公开 API：`render_variant`、`AugmentConfig`、`prediction_target`、`target_to_ir`、`variant_report`、`from_markush_json`/`to_markush_json`。

**M1 目标**：实现显式 NODE AR baseline（`方案.md` §5.2、§15.2），跑通训练/评测闭环，验证 v2 契约可学习。

**M1 退出标准**（`方案.md` §20）：
1. 单 batch 过拟合（loss 趋 0，重建图与 GT 一致）；
2. `--debug` 训练在 CPU 上可复现；
3. oracle edge F1 明显优于「SMILES 直接扩词表」基线，rich-label/placeholder 上尤其明显。

**非目标**：set-based learned queries、Hungarian 训练、拓扑感知 matching、真实专利微调、CXSMILES 主目标、立体化学完整监督。

**已锁定决策**：
- 环境：**CPU debug**，小 backbone、输入 384×384、batch 2–8。
- Stage 1 数据：**写 `mol_to_ir`**，用 RDKit 生成普通分子 IR 子集再渲染。
- 坐标：`x_bin/y_bin` 作为序列 token；**offset 回归与坐标解码放节点 embedding 头上**（与 §15.2 `gt_or_pred_coords` 一致）。

---

## 1. 环境与依赖

新建独立 venv（勿污染 `../MarkushRender/.venv`）：

```bash
cd MarkushScribe
python3.13 -m venv .venv
.venv/bin/pip install -U pip
.venv/bin/pip install torch --index-url https://download.pytorch.org/whl/cpu
.venv/bin/pip install timm numpy pillow rdkit pyyaml tensorboardX pytest ruff
.venv/bin/pip install -e ".[dev]"   # 或 PYTHONPATH=. 运行
```

`pyproject.toml` 新增可选依赖组 `ml = ["torch", "timm", "pyyaml", "tensorboardX"]`。
测试统一：`.venv/bin/python -m pytest -q` 与 `.venv/bin/ruff check .`。CPU 下所有模型必须能前向；耗时测试用 `pytest.mark.slow` 标记并可 `-m "not slow"` 跳过。

---

## 2. 数据契约速查（`markush_graph_v2`）

来源：`markushrender.target.prediction_target`，校验：`markushscribe.schema.parse_target`。

- 顶层：`format`、`coordinate_system`（normalized [0,1], origin top-left, y down）、`image_size [w,h]`、`coord_bins`（默认 64）、`order="reading"`、`nodes`、`edges`、`graph_annotations`、`masks{cxsmiles, normalized_chemistry_complete}`。
- 节点：`id`、`node_class ∈ {atom,rgroup,variable,abbreviation,ring_placeholder}`、`variable_subtype ∈ {ring_atom,linker,bond,generic}|null`、`anchor{x,y,rule}`、`coord_target{bins,x_bin,x_offset,y_bin,y_offset}`、`visual_label{visible,plain_text,runs[{text,script}]}`、`semantic_label{...}`、`loss_mask{field:bool}`、`placeholder{shape,radius}`（仅 placeholder）。
- 边：`source`、`target`、`adjacent`、`normalized{order∈{1,2,3}, aromatic:bool, kekule_order∈{1,2,null}}`、`depicted{type,visible_order,ring_mode}`、`variable:bool`、`wedge_direction ∈ {none,source_to_target,target_to_source}`。
- `depicted.type ∈ {plain,double,triple,companion,circle,solid_wedge,dashed_wedge,wavy,dotted,variable}`。
- `script ∈ {base,sub,sup}`；`anchor.rule ∈ {bond_vertex,base_core,placeholder_center}`；`ring_mode ∈ {edge,cross}`。

> 注意：`visual_label.runs` 是**视觉**真值；`semantic_label` 是规范语义。M1 只监督 `visual_label` + 闭集属性，semantic 由确定性 parser 派生（§6.5）。

---

## 3. `mol_to_ir`：普通分子 → MarkushRender IR

新增 `markushscribe/chemistry/mol_to_ir.py`（新建 `chemistry/` 包）。

目的：为 Stage 1 提供「普通 OCSR」训练对（全原子显式、无 R/变量）。

```python
def mol_to_ir(mol: Chem.Mol, *, source_smiles: str = "", source_id: str = "") -> dict:
    """RDKit Mol -> MarkushRender IR dict (nodes/edges/_meta), no coordinates."""
```

规则：
- 取最大重原子片段（复用 `markushgen.molecule.largest_fragment`）。
- 每个原子一个 `kind="atom"` 节点：`symbol/ element / h_count / charge / isotope / aromatic`（`symbol` 用 `markushgen.ir.atom_symbol` 构造，保证 Render 可无 `element` 解析）。
- 每个键一条边：`bond_type ∈ {single,double,triple,aromatic,solid wedge,dashed wedge}`（`molutil.bond_type`）；立体：
  - M1 可先不处理楔形（`@/@@` 丢弃，记 `_meta.stereo_dropped=true`）；后续可 RDKit 计算 2D + wedging。
- 不写坐标，交给 CoordGen（`engine=auto`）。
- `_meta`：`generator="markushscribe.chemistry.mol_to_ir"`、`source_smiles`、`source_id`、`canonical_smiles`、`scaffold_smiles`、`node_kind_counts`、`ir_version=1`、`stereo_dropped`。
- 校验：节点 id 连续、边端点存在、无自环。

测试（`tests/test_mol_to_ir.py`）：苯/咖啡因/含电荷/含同位素/含双键三键芳香，`mol_to_ir → markushrender.render(...,rasterize=False) → prediction_target → schema.parse_target` 全部通过；round-trip `target_to_ir` 节点数一致。

CLI（可选）：`tools/build_plain_corpus.py`，`--smi --out --shard-size`，复用 `markushgen.cli` 的 shard writer。

---

## 4. Tokenizer 与词表

新增 `markushscribe/constants.py` + `markushscribe/tokenizer.py`。

### 4.1 序列文法（节点按阅读序，§5.2）

```
<bos>
<node> <kind_xxx> <subtype_xxx|none> <xbin_k> <ybin_k>
       <label> <base>? <sub>? <sup>? ... </label>
</node>
... (N 个)
<eos>
```

- 每个节点固定前缀：`<node> <kind> <subtype> <xbin> <ybin> <label>`，label 内为 script/字符 token，最后 `</label> </node>`。
- `xbin/ybin` 为 64 档（token id 连续），offset 不进序列。
- 非 variable 节点的 subtype 用 `<subtype_none>`。
- `abbreviation`/`ring_placeholder` 的 label 直接来自 `visual_label.runs`（含 `(R7)m`）。
- 边缘情况：不可恢复字符 → `<unk>`；空 label（隐式 C）→ `<label></label>`。

### 4.2 词表分组（`constants.py`）

| 组 | 内容 | id |
| --- | --- | --- |
| special | `<pad>=0,<bos>,<eos>,<unk>,<node>,</node>,<label>,</label>` | 固定 |
| script | `<base>,<sub>,<sup>` | 固定 |
| kind | `<kind_atom>…<kind_ring_placeholder>`（5） | 固定 |
| subtype | `<subtype_none>,<subtype_ring_atom>,<subtype_linker>,<subtype_bond>,<subtype_generic>` | 固定 |
| bin | `<xbin_0>…<xbin_63>,<ybin_0>…<ybin_63>` | 固定 |
| char | 字母/数字/`()+-`/标点（数据统计，低频归 `<unk>`） | 数据 |

- `build_vocab(targets, min_freq=1) -> Vocab`，可 JSON 存取（`vocab.json`），保证训练/推理一致。
- `encode_target(target) -> EncodedSample`（token ids + node 切片索引 `node_token_positions` + 每节点 label token ids）。
- 往返：`decode_tokens(ids, vocab)` 能重建 `visual_label.runs` 与 kind/subtype/bin，断言结构一致。

### 4.3 闭集属性目标（不进序列）

`node_kind(5)`、`variable_subtype(5, masked)`、`visible(2)`、`charge_bucket(8)`、`h_count_bucket(7)`、`isotope_present(2)`、`placeholder_shape(4, masked)`（§6.2）。仅当 `loss_mask[field] != False` 且字段适用时计损失。

---

## 5. torch Dataset 与 collate

扩展 `markushscribe/dataset.py` 或新增 `markushscribe/torch_data.py`（推荐后者，避免污染 CPU-only 依赖）。

### 5.1 `TargetDataset`（map-style）

```python
class TargetDataset(Dataset):
    def __init__(self, shard_paths, vocab, image_size=384, q_capacity=128, t_capacity=32)
    def __getitem__(self, i) -> dict  # 见下表
```

- 索引：扫描 shard 建立 `(shard_path, stem)` 列表并缓存（首次构建 `index.json`）。
- 图像：PNG bytes → RGB → `Resize((384,384))`（各向异性，归一化坐标不变）→ float tensor `[3,384,384]`，`/255`，ImageNet 均值方差。
- **不改变归一化坐标**；推理时如需反变换，仅需乘回原图宽高。

### 5.2 每样本张量契约

| 键 | 形状/类型 | 说明 |
| --- | --- | --- |
| `image` | `float32 [3,384,384]` | 模型输入 |
| `tokens` | `int64 [L]` | `<bos>…<eos>`，最长 `L_max` |
| `token_mask` | `bool [L]` | padding 位置 False |
| `node_token_index` | `int64 [N]` | 每节点前缀（`<node>`）在序列中的位置；用于 AR 节点 gather 与对齐 |
| `node_kind` | `int64 [N]` | 闭集 |
| `node_subtype` | `int64 [N]` | 含 `<subtype_none>` |
| `node_visible` | `int64 [N]` | 0/1 |
| `coord_bin` | `int64 [N,2]` | x/y bin（与 token 内 `<xbin>/<ybin>` 一致，冗余但便于头监督） |
| `coord_offset` | `float32 [N,2]` | `[-0.5,0.5]` |
| `label_tokens` | `int64 [N,T]` | 每节点 label token（含 `<label>`/`</label>`） |
| `label_mask` | `bool [N,T]` | 有效 token |
| `node_mask` | `bool [N]` | 有效节点 |
| `edge_adjacency` | `float32 [N,N]` | GT 邻接（对称） |
| `edge_order` | `int64 [N,N]` | 1/2/3（非边 mask） |
| `edge_aromatic` | `int64 [N,N]` | 0/1 |
| `edge_depiction` | `int64 [N,N]` | depicted 类 |
| `edge_variable` | `int64 [N,N]` | 0/1 |
| `edge_wedge_direction` | `int64 [N,N]` | none/i→j/j→i（对称头只留上三角，见 §7） |
| `edge_mask` | `bool [N,N]` | 邻接的损失 mask（非边为 False） |
| `field_mask` | `dict[str,bool]` | 透传 per-field mask（h_count/charge…） |
| `meta` | `dict` | `uid`、原始 `image_size`、`source_smiles` |

- `N > q_capacity` 或 `T > t_capacity`：标记 `capacity_exceeded` 并由训练器跳过/告警，不静默截断（§15.3）。
- 边目标在 **GT 对齐节点** 上构造（M1 首版），`wedge_direction` 存成反对称矩阵：`m[i,j]=1, m[j,i]=?`；训练只算上三角并做方向 CE。
- `masks.cxsmiles` 无关，M1 忽略。

### 5.3 collate

`collate_fn(batch)`：按 batch 内 `L_max/N_max/T_max` padding，输出 batch 维；`token_mask/node_mask/label_mask/edge_mask` 同步。提供 `tests/test_torch_data.py`：形状、padding、mask、空图/单节点/N>T 边界。

---

## 6. 模型（`markushscribe/model/`）

常量：`D=256`、`Q=128`（容量）、`T=32`、`K=16` 沿线采样点、输入 384。

### 6.1 `encoder.py`

- timm backbone，多尺度特征（1/8、1/16、1/32）；输出 `memory [B,F,D]` + `pyramid`（列表，供 ROI/沿线采样）。
- 配置 `encoder.name`：默认 `swin_tiny_patch4_window7_224`（可与 MolScribe 对齐）；CPU debug 可切 `resnet18`/`convnext_tiny`。
- 位置编码尺寸不匹配时插值（§13.7）。

### 6.2 `node_ar.py`

- 标准 Transformer decoder（`nn.TransformerDecoder` 或自写），teacher forcing：`ar_decoder(tokens[:, :-1], memory) → token_hidden [B,L-1,D]`；`token_head` 输出 `[B,L-1,V]`。
- `gather_at_node_end(token_hidden, node_token_index) → node_hidden [B,N,D]`（§15.2）。
- 训练后期按课程把 `node_token_index` 换成**预测序列解析出的节点位置**，避免 teacher forcing 落差（§15.2）。

### 6.3 节点头（`node_heads.py` 或并入 model）

在 `node_hidden` 上：`kind_logits [B,N,5]`、`subtype_logits [B,N,5]`、`visible_logits [B,N,2]`、`x_bin_logits [B,N,64]`、`y_bin_logits [B,N,64]`、`coord_offset [B,N,2]`（tanh 到 [-0.5,0.5]）、闭集属性头（charge/h/isotope/placeholder_shape，masked）。

### 6.4 label decoder（`label_decoder.py`）

- 每节点小型 AR decoder，输入 `node_hidden` + 局部 ROI 特征（对 `pyramid` 做 ROIAlign/bilinear crop），teacher forcing 生成 label token。
- 输出 `label_logits [B,N,T,V_lbl]`；按节点平均损失（§12）。

### 6.5 edge heads（`edge_heads.py`，因子化，§7）

- pair feature：
  ```
  [q_i, q_j, q_i*q_j, |q_i-q_j|,
   dx,dy,dist,sin,cos, log(dist/bond_len_est),
   沿线采样 K=16 的 pyramid 特征（主线 + 法向窄带）]
  ```
- 头：`adjacency(2)`、`order(4: single/double/triple/other)`、`aromatic(3: false/true/unknown)`、`depiction(9)`、`variable(3)`、`wedge_direction(4: none/i→j/j→i/unknown)`。
- 只对 positive/candidate pair 算完整头；对称头显式对称化；`L_sym` 约束（§7.3）。
- `bond_len_est`：GT 可见单键中心距中位数（train 统计，配置固定）。

### 6.6 `model.py`

组装 forward，返回 dict（logits + aux），并提供 `compute_losses(pred, batch) -> dict[str, Tensor]`。

**MolScribe 初始化** `model/init.py`：加载 encoder 权重（及可选 decoder attention/FFN）；打印 loaded/missing/unexpected 与参数比例；测试断言「至少 encoder 加载比例 > 阈值」，防假加载（§13.7）。

---

## 7. 损失与掩码

`losses/node.py`、`losses/label.py`、`losses/edge.py`，总损失 `training/trainer.py` 汇总（§12）：

```
L = λ_node·L_node + λ_coord·L_coord + λ_label·L_label + λ_attr·L_attr
  + λ_adj·L_adj + λ_bond·L_bond + λ_dep·L_dep + λ_var·L_var + λ_wedge·L_wedge
  + λ_sym·L_sym
```

- `L_node`：kind CE（matched/序列节点）+ objectness 先保留（NODE AR 恒 node）。
- `L_coord`：`CE(x_bin)+CE(y_bin)+SmoothL1(offset)+L1(解码坐标, GT)`（§9.2）。
- `L_label`：token CE，按节点平均；`<pad>` mask。
- `L_attr`：仅适用且已知字段。
- `L_adj`：加权 BCE / focal；`L_bond/aromatic/depiction/variable/wedge`：条件于 adjacency，非边 mask。
- `L_sym`：对称头 KL + wedge 反向 CE（§7.3）。
- 初始权重全 1，先观察未加权梯度量级再调；记录每项有效样本数（§12）。配置写入 `configs/train_node_ar.yaml`。

---

## 8. 训练课程（M1 内）

`training/trainer.py` + `training/curriculum.py`。

1. **Stage 1 OCSR 适配**：`mol_to_ir` 生成普通分子 IR → 渲染 → v2；冻结 encoder，只训新头；无 placeholder/variable 字段 mask。
2. **Stage 2 合成 Markush**：`data/rendered` 全量字段；增强概率由低到高（先关模糊/极端旋转）。
3. **Teacher forcing → predicted nodes**：先用 GT `node_token_index`；随后以递增概率用**模型解码序列**解析出的节点位置替换（§15.2），用 validation node recall + anchor error 门控。
4. **replay**（可延后）：混入普通 OCSR 30–50%（§13.5）。

训练器要求：`--debug`（单/少量样本、固定 seed、关闭 dropout、batch 2–8、CPU）、checkpoint 存取（`training/checkpoint.py`，含 vocab/config/optimizer）、tensorboard/JSON 日志。**第一条验收：单 batch 过拟合。**

---

## 9. 评测（`evaluation/` + `matching/`）

- `matching/hungarian.py`：带属性的最优二分匹配（坐标 + kind + label + 拓扑），用于节点对应；`costs.py` 定义 cost。
- `evaluation/isomorphism.py`：Graph Exact（语义 / 语义+depicted / rich-label strict），忽略 id 与顺序（§18.1）。
- `evaluation/spatial.py`：node P/R/F1、kind acc、anchor mean/median/P90、bond-length 归一化误差、阈值召回（§18.2）。
- `evaluation/labels.py`：visual script exact、plain exact、script-aware 编辑距离、base/sub/sup F1、semantic exact、R/M/L family/index、multiplicity、charge/isotope/H、abbreviation CER（§18.3）。
- `evaluation/edges.py`：**oracle edge F1**（GT 节点/坐标）与 **end-to-end edge F1**（预测节点匹配后）分开；adjacency/order/aromatic/depiction/variable/wedge（§18.5）。
- placeholder 指标单独报告（§18.4）。
- “SMILES 直接扩词表”基线：M1 需一个最简基线与上述指标对比；可先用 MolScribe 预训练直接跑普通 OCSR 子集作为参照。

---

## 10. 推理与组装（`inference/`）

`decode.py`：给定模型输出，解析节点序列（或接管 AR 生成）→ 节点列表 + 标签 + 属性；`assemble.py`：对称化、去 self-edge、wedge 反向映射、解析 semantic、输出 v2 图；`confidence.py`：节点/边置信度。
复用 `visualization/overlay.py` 做预测 overlay；新增 `tools/inspect_predictions.py`。

后处理只做确定性操作，禁止价态自动改键级/删特殊节点（§16.2）。

---

## 11. 配置、checkpoint、日志

`configs/`：
- `model_node_ar.yaml`：encoder/backbone、D、Q、T、K、image_size、heads。
- `train_node_ar.yaml`：stage、loss 权重、lr、schedule、batch、augment 概率、data paths、seed。
- `train_node_ar_debug.yaml`：CPU 单 batch 过拟合用。

checkpoint 存 `{model, optimizer, epoch, vocab, config, git_hash?}`；权重目录 `ckpts/`（不入库）。

---

## 12. 测试清单（每模块验收）

| 文件 | 覆盖 |
| --- | --- |
| `tests/test_mol_to_ir.py` | 普通分子 IR 字段、渲染+target 往返、空/单原子 |
| `tests/test_tokenizer.py` | 序列文法、词表构建/存取、base/sub/sup、`(R7)m`、`<unk>`、往返一致 |
| `tests/test_torch_data.py` | 形状、padding/mask、空图/单节点/多 R/N>T capacity |
| `tests/test_model_shapes.py` | encoder/node_ar/heads/edge_heads 前向形状、mask 传播、packed vs padded |
| `tests/test_losses.py` | 各项 loss 在退化输入下的有限性与 gradient 非零；全 mask 不 NaN |
| `tests/test_matching.py` | Hungarian 对应、对称/交换、cost 单调性 |
| `tests/test_overfit.py` | 单 batch 过拟合 loss→0（标 `slow`） |
| `tests/test_init.py` | MolScribe 加载键报告、加载比例断言 |
| `tests/test_eval.py` | exact/F1 在构造样例上的正确性 |

---

## 13. 工作分解与顺序（T1–T7）

| 阶段 | 交付 | 依赖 | 验收 |
| --- | --- | --- | --- |
| **T1** | venv + torch/timm；`constants.py`；`tokenizer.py`；`test_tokenizer.py` | — | 往返测试、`ruff`、pytest |
| **T2** | `chemistry/mol_to_ir.py`；`tools/build_plain_corpus.py`；`test_mol_to_ir.py` | T1 | 普通分子渲染+target 通过 |
| **T3** | `torch_data.py`（Dataset/collate）；`test_torch_data.py` | T1 | 形状/mask/边界 |
| **T4** | `model/encoder.py` + `node_ar.py` + 节点头；`test_model_shapes.py` | T3 | 前向形状；单 batch 过拟合（无 label/edge） |
| **T5** | `model/label_decoder.py`；`losses/label.py` | T4 | label 过拟合 |
| **T6** | `model/edge_heads.py`；`losses/edge.py`；`L_sym` | T4 | oracle edge F1 可学；过拟合含边 |
| **T7** | `training/*`、`configs/*`、`evaluation/*`、`matching/*`、`inference/*`、`model/init.py`、`tools/inspect_predictions.py` | T2–T6 | 退出标准：单 batch 过拟合 + `--debug` 复现 + 基线对比 |

建议先 T1→T3→T4 打通「能前向、能过拟合」的最小闭环，再补 T5/T6/T2/T7。

---

## 14. 风险与开放项

- **CPU-only**：只验证可学习与契约自洽，不追指标；正式训练需 GPU。
- **隐式 C**：NODE AR 用序列「何时发节点」学习，缺少 set query 的 objectness，是 M1 最大不确定点；必要时加节点中心 heatmap 辅助头（§6.3）。
- **缩写长尾**：`<unk>` 与 `T` 截断需单独报告（§18.3）。
- **MolScribe 词表**不兼容，仅迁移 encoder；加载比例要有测试。
- **坐标系**：各向异性 resize 不改变归一化坐标；若改 letterbox 必须重映射 anchor。
- **开放项**：是否在 M1 就做 teacher forcing → predicted 课程（建议先只放接口，验证集门控开启）；普通 OCSR 基线具体形式待定。

---

## 15. 参考

- 设计依据：`方案.md` §5.2（NODE AR）、§6（节点 schema）、§7（边头）、§9（坐标）、§11（matching）、§12（损失）、§13（课程）、§15.2（张量）、§16（推理）、§18（评测）、§20（M1）。
- 现有代码：`markushscribe/{schema,dataset,transforms,split,visualization/overlay}.py`、`markushscribe/markushgen/`。
- MolScribe（只读）：`MolScribe/molscribe/{model,tokenizer,chemistry,interface}.py`。
- MarkushRender：`markushrender/{renderer,augment,target,styles,ir}.py`。
- M0 报告：`docs/m0_report.md`；数据管线：`docs/data_pipeline_plan.md`。

---

## 16. 实施记录（v1，2026-09）

### 16.1 环境

- 新建 `.venv`（未污染 `../MarkushRender/.venv`）：`torch==2.14.0+cpu`（经 SJTU CPU 镜像
  `https://mirror.sjtu.edu.cn/pytorch-wheels/cpu`，`download.pytorch.org` 在本机不可达）、
  `torchvision==0.29.0+cpu`、`timm==1.0.29`、`rdkit`、`pillow`、`numpy`、`pyyaml`、
  `tensorboardX`、`pytest`、`ruff`；`pip install -e ../MarkushRender` 供渲染真值。
- `pyproject.toml` 新增 `ml` 可选依赖组与 pytest `slow` marker。

### 16.2 新增模块

| 文件 | 内容 |
| --- | --- |
| `markushscribe/constants.py` | 词表分组、kind/subtype 枚举、坐标 bin、闭集属性表、bucket 函数 |
| `markushscribe/tokenizer.py` | `Vocab`、`build_vocab`、`encode_target`、`decode_tokens`（抗截断） |
| `markushscribe/chemistry/mol_to_ir.py` | RDKit Mol → Markush IR（Stage 1） |
| `markushscribe/torch_data.py` | `TargetDataset`、`build_sample`、`collate_fn`、`normalize_image` |
| `markushscribe/model/{encoder,node_ar,node_heads,label_decoder,edge_heads,model,init}.py` | 模型与权重加载 |
| `markushscribe/losses/{common,node,label,edge}.py` | 掩码损失与 `compute_losses` |
| `markushscribe/matching/{hungarian,costs}.py` | 无 SciPy 依赖的匈牙利匹配 + 节点代价 |
| `markushscribe/evaluation/{labels,edges,metrics}.py` | 节点/标签/边/oracle & e2e 指标、graph exact |
| `markushscribe/inference/{decode,assemble,confidence}.py` | 贪心解码、组装、置信度 |
| `markushscribe/training/{config,checkpoint,curriculum,trainer}.py` | 配置、ckpt、教师强制课程、训练循环 |
| `markushscribe/train.py` / `predict.py` / `evaluate.py` | 训练 / 预测 / 评测 CLI |
| `tools/{build_plain_corpus,inspect_predictions,verify_overfit}.py` | Stage 1 语料 / 预测 overlay / 小规模过拟合验证 |
| `configs/{model_node_ar,train_node_ar,train_node_ar_debug,train_node_ar_stage1}.yaml` | 配置 |
| `tests/{...,test_evaluate_runner,test_graph_to_smiles}.py` + `tests/conftest.py` | 154 tests |

### 16.3 验证

```bash
.venv/bin/ruff check .                 # clean
.venv/bin/python -m pytest -q          # 154 passed
.venv/bin/python -m markushscribe.train --config configs/train_node_ar_debug.yaml --debug
.venv/bin/python -m markushscribe.train --config configs/train_node_ar_debug.yaml --debug --overfit --steps 250
.venv/bin/python -m markushscribe.predict --ckpt runs/node_ar_debug/last.pt --image <png>
.venv/bin/python -m markushscribe.evaluate --ckpt <ckpt> --shard data/plain_rendered/valid/shard_*.tar --limit 20
python tools/inspect_predictions.py --ckpt runs/node_ar_debug/last.pt --shard data/rendered/valid/shard_00000.tar --out /tmp/inspect --limit 2
```

- Stage 1 数据已生成：`tools/build_plain_corpus.py` → `data/plain_ir`(2000)，`build_render_dataset` →
  `data/plain_rendered`(train 1960/valid 20/test 20，0 失败/空白/重叠)；`validate_dataset --strict` 全绿。
- 评测 runner `markushscribe/evaluate.py` 已加（汇总 `summary.json` + `per_sample.jsonl`，`--max-len`
  控制解码上限）；`configs/train_node_ar_stage1.yaml` 冻结 encoder 冒烟跑通（1 epoch/32 样本）。

- 退出标准 1（单 batch/小规模过拟合）：
  - 2 条（31 节点）：800 步 `34.98 → 0.0013`，`node_f1=edge_f1=1.00`、`graph_exact=1`；
  - 10 条（18–42 节点）：343 步 `36.08 → 0.0100`（early stop），逐条 `graph_exact=1`，合计 **10/10**；
    复现 `python tools/verify_overfit.py --limit 10 --steps 800 --tolerance 1e-2 --eval`。
- 退出标准 2（`--debug` CPU 可复现）：两次 debug 运行除 `seconds` 外逐项一致。
- 单测 `tests/test_overfit.py`（slow）在 250 步内 `loss[-1] < loss[0] * 0.5`。
- 退出标准 3（优于 SMILES 扩词表基线）：基线采用**复用 MolScribe 预训练权重**（`tools/run_molscribe_baseline.py`，
  离线 shim 旧 timm/onmt，不改 `MolScribe/`）。在 valid 各 20 条上：普通 OCSR `exact=0.50 / tanimoto=0.55`，
  Markush `exact=0.00 / tanimoto=0.09`。即基线在 Markush 上几乎完全失败；**NODE AR 侧同 shard 指标待正式训练后补齐**。
  `markushscribe/chemistry/graph_to_smiles.py` 提供 gold 图的 canonical SMILES 以支持该比对。
- 修复记录：`assemble_graph` 曾把 `[N,N,2]` 邻接 logits 误当带 batch 维并剥掉首维，导致
  `predict` 索引越界；现支持 `[N,N,2]` logits 与 `[N,N]` 概率两种输入，并接入 aromatic 头，
  回归测试见 `tests/test_inference.py::test_assemble_graph_accepts_edge_logits_and_aromatic`。

### 16.4 与方案的偏差 / 后续

- 节点 embedding 在 **`<node>` 起始位置** gather（而非 `</node>`），使 kind/subtype/坐标头在该位置
  预测紧随其后的 token，天然无标签泄漏；节点内容由 label decoder 的 `label_summary` 补入 `node_features`。
- 边头当前只用 **节点特征 + 几何**，尚未实现沿线 `K=16` 视觉采样与 ROI；`use_line_features` 待加。
- label decoder 为并行小 Transformer（`[B*N]` 展平 + 单条件 token 交叉注意力），未做 ROIAlign。
- `model/init.py` 为通用形状匹配加载器（前缀/后缀匹配 + 报告）；MolScribe 编码器逐键映射仍需核对其
  `state_dict` 命名后调优，当前仅保证「不假加载」。
- `evaluation` 的 `graph_exact` semantic 模式忽略 depicted；strict 模式才检查 visual label 与 wedge。
- 正式训练前需：`tools/build_plain_corpus.py` 生成普通分子 IR → `build_render_dataset` 渲染；
  GPU 上将 backbone 换 Swin、384 输入、batch 2–8；再开启 `teacher_forcing_epochs`。

