# MarkushScribe 完整设计方案草案

> 文档状态：权威方案草案（供项目设计、数据契约、实现与验收使用）  
> 目标版本：`markush_graph_v2`  
> 适用范围：输入一张已经裁剪好的纯 Markush 结构图，输出图中可见的结构骨架、富标签语义、语义锚点与边  
> 不适用范围：结构图外的变量定义表、专利正文、`variable_def`、`rgroup_definitions` 的识别

---

## 0. 文档口径

本文把内容分成两类，避免把论文结论、现有代码行为和尚未实现的方案混为一谈。

- **[事实]**：已由论文、公开实现或当前仓库代码直接确认的行为。
- **[建议]**：本项目推荐采用、仍需通过实验验证的设计。
- **[约束]**：项目实现和数据必须遵守的契约；修改时需要同步修改本文、schema、转换器和测试。

本文同时描述目标设计和当前配套数据导出能力。尤其需要明确：

- `MarkushRender/markushrender/target.py` 已实现本文的 `markush_graph_v2` 基础导出：visual / semantic 双标签、semantic anchor、64-bin + offset、独立 ring-placeholder 类型及 normalized / depicted 边。
- `target_to_ir()` 只恢复能映射进 MarkushIR 的**结构子集**；它刻意不恢复定义字段、任意 metadata、原始 node id 或 Style 级芳香画法。
- 只有对 v2 明确列入模型目标、且能映射回 IR 的字段，经过严格测试后，才能称为“预测子集可恢复”；不得外推为对原始 IR 的完全无损往返。

---

## 1. 问题定义

### 1.1 项目目标

**[约束]** MarkushScribe 接收一张纯 Markush 结构图，识别图中实际可见的结构信息，输出一个不依赖 SMILES 线性顺序的结构化图：

1. 全部结构节点，包括可见原子、隐式碳顶点、R 基团、M/L 变量、缩写和 A/B 圆圈占位环。
2. 每个节点的二维**语义锚点**，归一化到输入图像坐标。
3. 每个可见节点的完整标签语义，包括字符、`base/sub/sup`、括号、多重度、同位素、电荷和缩写原文。
4. 每条边的邻接关系、化学语义与实际画法，包括键级、芳香性、波浪/虚线/楔形等 depiction、变量属性和楔形方向。
5. 同时保留“图上画成什么”和“化学上规范成什么”，尤其对芳香键不得只保留其中一个。
6. 输出可直接用于图同构评测、人工修订、渲染回放和下游导出。

核心任务可写成：

```text
image -> unordered semantic nodes + anchors + rich labels + factorized edges
```

它首先是 **image-to-graph**，其次才是可选的 graph-to-CXSMILES 导出问题。

### 1.2 非目标

**[约束]** 第一阶段明确不做：

- 不识别结构图外的 `variable_def` 或 `rgroup_definitions`。
- 不识别专利正文、脚注、定义表、权利要求或图注。
- 不引入完整文档 OCR、Vision-Text-Layout（VTL）编码器或大型通用 VLM 作为首期必需依赖。
- 不要求模型直接生成完整、唯一、标准化的 SMILES/CXSMILES 字符串。
- 不负责根据文字定义枚举具体分子，不做 Markush 命中、检索或权利要求推理。
- 不把化学合法性修复当成识别结果；修复建议必须与原始预测分开保存。
- 不要求复原不可从像素确定的信息，例如变量的允许取值集合。

### 1.3 为什么不能简单照搬 SMILES 线性化

Markush 结构中的图语义与一条 SMILES 字符串不等价：

- 同一张图可有多种合法遍历顺序和多种等价字符串，造成无意义的序列监督冲突。
- `R1`、`M2`、`L1`、`(R7)m` 和圆圈 `A/B` 不是普通元素 token。
- 标签的上下标、括号和多重度是像素中存在且必须识别的语义，普通 SMILES 不保留排版层次。
- 圆圈占位环是一个语义节点，不应被强制展开成未知数量的普通原子。
- 可变键、位置变化、画法差异和规范芳香性难以压入一个无歧义的普通 SMILES 序列。
- 序列前部错误可能改变后续节点身份，从而连带破坏边头的节点对齐。

因此主表示必须是图；CXSMILES 只作为辅助训练信号、兼容导出和次级评测。

---

## 2. 输入输出契约

### 2.1 输入

**[约束]** 单样本输入至少包含：

```python
image: FloatTensor[3, H, W]  # RGB，预处理后通常为 384x384 或 512x512
image_id: str
original_size: tuple[int, int]  # (width, height)
```

数据加载器还应携带但不喂给模型的审计信息：

```python
source_id: str
canonical_structure_id: str
scaffold_id: str | None
patent_family_id: str | None
render_seed: int | None
split: Literal["train", "valid", "test"]
```

输入图应只包含结构图本体。若原始页面同时包含变量定义表，应先由外部裁剪/分区模块提供结构图区域；本模型不依赖定义表才能工作。

### 2.2 输出

**[约束]** 推理 API 输出 `markush_graph_v2`：

```python
{
  "format": "markush_graph_v2",
  "coordinate_system": "normalized [0,1], origin top-left, y down",
  "image_size": [width, height],
  "nodes": [...],
  "edges": [...],
  "auxiliary": {
    "cxsmiles": str | None,
    "cxsmiles_valid": bool | None,
    "warnings": list[str]
  }
}
```

输出遵守以下原则：

- 节点 `id` 只要求在当前样本内唯一；集合式模型的 query 序号不是持久语义。
- 边通过节点 `id` 引用节点，不通过标签字符串引用。
- 坐标永远对应输入图，而不是原始 IR 的布局初值。
- 原始预测、阈值化结果与化学后处理结果应可分开取得。
- `variable_def` 和 `rgroup_definitions` 不出现在模型主输出中。

### 2.3 成功定义

一张图识别成功，不等于能导出一个 RDKit 可接受的普通分子。主成功条件是：

1. 节点集合正确。
2. 节点富标签与节点类别正确。
3. 节点锚点与图上语义位置对齐。
4. 边连接与各因子正确。
5. 在容许的节点重排下，预测图与真值图同构。

---

## 3. 参考系统：MolScribe

### 3.1 已确认的准确事实

**[事实]** 当前仓库内 MolScribe 参考实现是 image-to-graph 模型，常用图像编码器为 Swin-B，解码器为 6 层 Transformer，自回归生成节点相关序列，并由图头预测节点对之间的边。

**[事实]** 发布模型使用的关键目标之一是 `chartok_coords`：

- 主序列是字符级 SMILES。
- 每识别到一个原子 token，在该原子字符片段后追加离散 `x`、`y` 坐标 token。
- 推理接口默认 `coord_bins=64`，x/y 各量化为 64 档；x 和 y 使用可分离 token 区间。
- 字符级原子可能占多个字符，但坐标只跟在完整原子 token 后。
- `atom_indices` 指向每个原子在解码隐藏状态中的对齐位置，边头据此抽取 N 个原子表示。

**[事实]** MolScribe 的 `GraphPredictor` 对每个有序节点对拼接两端隐藏状态，输出 7 类 logits，张量为 `B×7×N×N`，语义为：

```text
0 no-bond
1 single
2 double
3 triple
4 aromatic
5 solid wedge
6 dashed wedge
```

楔形键类别有方向性；实现会在反向矩阵位置交换 solid/dashed 对应关系后做对称化。

**[事实]** `chartok_coords` 的 `max_len=480` 是**整个解码 token 序列长度上限**，不是原子数上限。字符、括号、环号、键符号、每个原子的 x/y token 和特殊 token 都占长度。因此不能把 480 解释为最多 480 个节点，也不能直接据此设置 MarkushScribe 的 query 数。

**[事实]** 训练时边头使用 teacher forcing 下的真值序列隐藏状态和真值 `atom_indices`；推理时使用模型自己生成的序列及预测出的原子位置。由此存在明确的 train-inference gap：

- 训练时节点数量、顺序、标签边界和对齐位置均正确。
- 推理时任一序列漏字、错字、提前 EOS 或原子边界错误，都会改变供边头使用的节点表示。
- 边头训练阶段没有自然经历这些 predicted-node 错误。

这不是说 MolScribe 的方案无效，而是说明 MarkushScribe 必须专门设计从 GT query 到 predicted query 的课程训练。

### 3.2 可复用部分

**[建议]** 可复用或部分初始化：

- 图像预处理、Swin/ConvNeXt/ViT 编码器接口。
- MolScribe 的 OCSR 视觉编码器 checkpoint，尤其是键线、环、字形与布局特征。
- 多尺度图像特征和位置编码思路。
- 节点对边预测范式以及边方向对称性处理。
- 普通 OCSR 数据、动态渲染和图像增强经验。
- 坐标离散化作为分类任务的稳定性经验。
- 普通分子图构建、化学校验与辅助导出工具。

### 3.3 不应直接复用的部分

**[建议]** 下列设计不适合作为最终主路线：

- 不直接把 SMILES 字符串作为 Markush 图的唯一节点载体。
- 不把 7 类边互斥分类原样扩展为越来越大的单一类别表。
- 不让节点身份依赖 SMILES 遍历位置。
- 不把 `max_len` 当作节点容量。
- 不只保留一个普通字符串标签；必须保留 rich label script。
- 不让边头永远只看到 teacher-forced 节点。
- 不依赖 RDKit 能完整表达全部 Markush 特殊节点后才输出结果。

---

## 4. 参考系统：MarkushGrapher-2

### 4.1 已确认的准确事实

**[事实]** MarkushGrapher-2 是面向多模态 Markush 识别的端到端 encoder-decoder 系统，其输入可包含结构图和文字定义，输出是 Markush 骨架 CXSMILES 与取代基表。

**[事实]** 它使用两条互补编码路径：

- OCSR 视觉路径：来自 MolScribe 的 Swin-B 视觉编码器，经 MLP 投影，主要提供结构骨架视觉特征。
- Vision-Text-Layout 路径：ChemicalOCR 提取字符级文本和边界框，再与图像 patch 一起输入基于 T5-base/UDOP 思路的 VTL 编码器。

两路表示拼接后送入文本解码器，自回归生成顺序表示。

**[事实]** 它采用两阶段训练：

- Phase 1 Adaptation：冻结视觉编码器，在 243K 真实 image-SMILES 对上训练 projector 与 text decoder，使解码器适应已预训练的 OCSR 特征空间。
- Phase 2 Fusion：引入 OCR 与 VTL 路径，冻结视觉编码器、projector 和 OCR，训练 VTL encoder 与 text decoder，目标为 CXSMILES 和 substituent table；论文报告使用 235K 合成与 145K 真实 Markush 样本。

**[事实]** 论文消融支持两点：视觉 OCSR 路径更擅长骨架，文本/布局路径对 Markush 特征重要；先适配再融合优于直接单阶段融合。

### 4.2 适用边界

MarkushGrapher-2 解决的是比本项目第一阶段更宽的问题：其关键难点包括结构图外或周边的文字定义、取代基表、位置和频率变化指示，以及 OCR 与布局融合。

本项目当前输入是**纯结构图**，并明确把 `variable_def/rgroup_definitions` 交给其他工作处理。因此：

- ChemicalOCR + VTL 双编码器不是 M0-M3 的必要条件。
- 831M 级多模态系统的工程和训练成本不符合首期目标。
- CXSMILES + 表格的长序列生成不能替代本项目的显式图监督。
- MarkushGrapher-2 的性能数字不能直接作为本项目图同构指标的可比基线，因为任务范围和输出不同。

### 4.3 可借鉴点

**[建议]** 借鉴的是训练原则，而不是照搬整套重装备：

- 保留一个对普通 OCSR 强的视觉分支，并尽量从 MolScribe 初始化。
- 先让新节点/边解码器适配已有视觉空间，再逐步解冻视觉编码器。
- 普通分子数据与 Markush 数据混训，避免专门化后遗忘普通化学结构。
- 合成预训练之后必须用真实专利图微调。
- 将来任务扩展到整页和定义表时，再增加 OCR/VTL 分支；主图 schema 保持不变。

---

## 5. 总体推荐架构

### 5.1 主路线：set-based node queries

**[建议]** 最终优先采用 DETR 风格的集合预测：

```text
image
  -> visual encoder + multi-scale features
  -> Q learned node queries
  -> transformer query decoder
  -> per-query objectness/class/anchor/closed attributes
  -> per-positive-query label-script decoder
  -> factorized pairwise edge heads
  -> graph assembly and optional chemical export
```

优点：

- 节点本来就是无序集合，训练目标与问题结构一致。
- Hungarian matching 显式解决 query 与 GT 节点对应，不依赖 SMILES 或阅读序。
- 漏一个标签字符不会使后续全部节点错位。
- 边头始终工作在清晰的 query 空间，便于从 GT query 平滑过渡到 predicted query。
- 可以独立评价节点检测、标签、坐标和边，易于定位误差。

初始建议 `Q=128`，但它是容量超参数，应根据训练集节点数 P99 加安全余量确定：

```text
Q >= ceil(1.25 * P99(number_of_nodes))
```

若超大结构常见，应采用 `Q=192/256`、动态裁块或层次图，而不是静默截断。

### 5.2 可先落地的 baseline：显式 NODE AR

**[建议]** 为尽快跑通全链路，可先实现不含 SMILES 语法的节点自回归基线：

```text
<bos>
<node> kind subtype x_bin x_off y_bin y_off <label> ... </label> </node>
...
<eos>
```

节点按稳定阅读序排列：先按 y 分行，再按 x，从上到下、从左到右。边仍由节点对图头预测，不在线性序列中枚举。

这个 baseline 的价值是验证：

- v2 schema 是否可学习。
- label-script tokenizer 是否覆盖真实标签。
- 边因子化是否优于 7 类互斥头。
- MolScribe 初始化是否有效。

其局限是仍有 exposure bias、顺序依赖和 `max_len` 截断。因此它是工程基线和消融对象，不是默认最终架构。

### 5.3 视觉特征

**[建议]** 初版使用 MolScribe Swin-B 或可替换的 Swin/ConvNeXt，输入 384×384；保留 1/8、1/16、1/32 多尺度特征。细小上下标和楔形线需要高分辨率层，不能只保留最终低分辨率特征。

若 384 输入导致下标不可辨，再比较：

- 512×512 全图。
- 384 全图 + query-guided label crop encoder。
- 特征金字塔 + ROIAlign 标签局部特征。

### 5.4 两类标签必须分开

**[约束]** 每个节点同时有：

- `visual_label`：图上实际可见的顺序与排版。例：根据键方向可能是 `HN` 或 `NH`；保留括号、下标、上标。
- `semantic_label`：与布局无关的规范语义。例：氮节点规范为 element=`N`, h_count=`1`；R 基团规范为 family=`R`, index=`7`, multiplicity=`m`。

两者不能合并，因为：

- OCR/视觉监督需要忠实描述像素。
- 图比较、化学导出和跨画法归一化需要稳定语义。
- 同一语义可能有多种视觉排列。

推荐字段：

```python
visual_label: {
    "runs": [{"text": "(", "script": "base"}, ...],
    "plain_text": "(R7)m",
    "visible": True,
}
semantic_label: {
    "canonical_text": "(R7)m",
    "family": "R",
    "index": "7",
    "multiplicity": "m",
}
```

### 5.5 semantic anchor

**[约束]** 节点坐标是语义锚点，不是字符框中心，也不是整串标签的几何中心。锚点定义必须与 MarkushRender 生成目标时使用的点一致。

| 节点 | anchor 定义 |
| --- | --- |
| 隐式 C | 所有相连键轴线在骨架顶点的交点；无可见标签 |
| 普通可见原子 | 元素核心字形中心，如 `N` 的中心；不取 `NH2` 整串中心 |
| R/M/L 变量 | 核心 family 字形中心，如 `R`、`M`、`L`；编号下标不拉动 anchor |
| `(R7)m` | 内部核心 `R` 字形中心；括号、编号和多重度不拉动 anchor |
| 缩写 | 语义连接核心的锚点；若没有更细标注，采用主 base run 的几何中心，并记录 `anchor_rule="base_core"` |
| A/B 圆圈占位环 | 圆/多边形占位符几何中心，而不是字母 ink 中心 |
| 单独显式 H | H 字形中心 |

缩写的连接核心有歧义时，训练数据必须提供一致规则；不得一部分取整串中心、一部分取连接端字符。

---

## 6. 节点语义 schema

### 6.1 最小无冲突类别

**[约束]** 闭集节点类型只表达互斥的顶层身份：

```text
node_kind =
  atom
  rgroup
  variable
  abbreviation
  ring_placeholder
```

变量子类只在 `node_kind=variable` 时有效：

```text
variable_subtype =
  ring_atom       # M/M1 等，作为环上节点
  linker          # L/L1 等，作为连接节点
  generic         # X/Y/Z/Hal/Ar 等开放变量
```

`ring_placeholder` 是独立顶层类，避免同时出现 `kind=variable`、`variable_type=ring`、`ring_placeholder=true` 三套重叠真值。A/B 的字母是占位符标识，不把它误分类成普通元素 B。

`rgroup` 与 `variable` 分开，因为 R 基团是取代点标签，而 M/L 等变量可作为环原子或连接子参与骨架。

### 6.2 闭集属性头

**[建议]** 对稳定、有限、可互斥的属性使用分类/多标签头：

```text
objectness: {node, no-object}
node_kind: 5 classes
variable_subtype: 3 classes, masked unless variable
is_visible: binary
is_aromatic_atom: binary/unknown
formal_charge_bucket: {-3,-2,-1,0,+1,+2,+3,other}
h_count_bucket: {0,1,2,3,4,other,unknown}
isotope_present: binary
placeholder_shape: {circle, polygon, bracket, unknown}, masked unless placeholder
```

元素本身可在常见元素闭集上设辅助头，但不能代替开放标签解码器。闭集头提供强先验和容易优化的损失；开放解码器负责长尾缩写、任意编号和罕见符号。

### 6.3 隐式 C 提议

**[约束]** 隐式碳必须作为真实节点进入 GT 和预测图：

```json
{
  "node_kind": "atom",
  "semantic_label": {"element": "C", "canonical_text": "C"},
  "visual_label": {"runs": [], "plain_text": "", "visible": false},
  "anchor": {"x": 0.42, "y": 0.31, "rule": "bond_vertex"}
}
```

模型需要从线段端点、折点和交点提议隐式 C，而不是只检测文字。为降低漏检：

- query decoder 使用高分辨率线特征。
- 增加 bond-junction heatmap 作为辅助任务。
- matching 对隐式 C 的坐标与拓扑成本适当加权。
- 训练集中保留纯烃和无标签环的充分比例。

### 6.4 圆圈占位节点提议

**[约束]** A/B 圆圈占位环是一个节点，不把圆周采样成多个原子：

```json
{
  "node_kind": "ring_placeholder",
  "semantic_label": {"placeholder_id": "A", "canonical_text": "A"},
  "visual_label": {"runs": [{"text": "A", "script": "base"}]},
  "anchor": {"x": 0.61, "y": 0.50, "rule": "placeholder_center"},
  "placeholder": {"shape": "circle"}
}
```

建议增加 placeholder center/radius 或 bbox 辅助头，仅用于定位和视觉 QA；主图契约只要求中心 anchor 和节点身份。

### 6.5 节点内 label-script decoder

**[建议]** 每个 positive query 使用共享的小型自回归 decoder 或并行字符 decoder，读取该 query 与局部 ROI 特征，生成：

```text
<label_bos>
<base> ( <base> R <sub> 7 <base> ) <sub> m
<label_eos>
```

token 设计：

- script 状态 token：`<base> <sub> <sup>`。
- 字符：字母、数字、括号、正负号、常见标点。
- 可选原子级 token：常见多字符元素或缩写片段，但解码必须能退化到字符级。
- `<unk>` 只用于不可恢复字符；评测时单独报告，不能静默吞掉。

开放 decoder 与闭集属性头的分工：

| 目标 | 推荐机制 | 原因 |
| --- | --- | --- |
| 顶层节点种类 | 闭集头 | 类别稳定、互斥 |
| variable subtype | 闭集头 | 类别少，直接影响拓扑语义 |
| 常见元素/电荷/H 数 | 闭集辅助头 + script | 增强稳定性，同时保留原文 |
| R/M/L 编号 | script + 规范解析 | 编号开放，不应固定到 R1-R12 |
| 多重度 m/n/x/y | script + semantic 字段 | 字符开放且上下标重要 |
| 缩写 | 开放 script | 长尾不可穷举 |
| 括号和上下标 | script | 属于可见排版语义 |

推理后由确定性 parser 将 `visual_label.runs` 和闭集属性融合成 `semantic_label`。发生冲突时保留两者和 warning，不得悄悄覆盖原始视觉预测。

---

## 7. 因子化边头

### 7.1 为什么不扩成一个大类别表

一条边可同时是“邻接 + 芳香规范语义 + 画成伴线 + 可变 + 有方向”。把所有组合做笛卡尔积会造成类别爆炸、长尾稀疏和无法表达新画法。

**[建议]** 对每个 query 对 `(i,j)` 构建 pair feature，并使用独立但条件相关的头：

```text
adjacency(i,j): binary
bond_order(i,j): {single,double,triple,other,unknown}
aromatic(i,j): {false,true,unknown}
depiction(i,j): {plain,wavy,dotted,solid_wedge,dashed_wedge,aromatic_companion,
                  aromatic_circle,unknown}
variable(i,j): {false,true,unknown}
wedge_direction(i,j): {none,i_to_j,j_to_i,unknown}
```

其中：

- 非邻接 pair 的其他损失 mask 掉。
- `bond_order` 与 `aromatic` 分离；芳香边可有 depicted single/double/companion，但 normalized aromatic 为真。
- `depiction` 描述图上证据，不替代化学语义。
- `wedge_direction` 使用语义节点方向，不依赖临时 query 顺序。约定方向指“楔尖/立构中心 -> 宽端”；无法确认时为 `unknown`，不可硬猜。
- `variable` 是独立布尔/三值属性，可与 wavy/dotted depiction 共存。

### 7.2 pair feature

**[建议]** 仅拼接两个 query embedding 不足以可靠区分交叉近邻和真实连接。pair feature 至少包括：

```python
pair_ij = concat(
    q_i,
    q_j,
    q_i * q_j,
    abs(q_i - q_j),
    relative_geometry(i, j),
    line_visual_feature(i, j),
)
```

相对几何特征：

```text
dx, dy, distance, sin(theta), cos(theta),
log(distance / estimated_bond_length),
endpoint visibility/type embeddings
```

沿线视觉特征：

- 在两 anchor 连线上采样 K 个 encoder feature，建议 `K=16` 或 `32`。
- 同时采样法向两侧窄带，捕获双键、芳香伴线、虚楔横档和波浪。
- 使用可微 bilinear sampling；可将主线、左带、右带汇聚成固定维度向量。
- 对很近的标签节点，采样应从标签边缘附近到另一端，而不是只看端点。

### 7.3 对称性与方向性

**[约束]** `adjacency`、`bond_order`、`aromatic`、`variable` 应满足 `(i,j)=(j,i)`。训练时可只计算上三角并显式对称化。

`wedge_direction` 是反对称关系：交换端点时 `i_to_j` 与 `j_to_i` 互换。`depiction=solid_wedge/dashed_wedge` 本身不承担方向。

推荐加入一致性损失：

```text
L_sym = KL(p_ij, p_ji) for symmetric heads
      + CE(reverse(p_wedge_ji), p_wedge_ij)
```

### 7.4 稀疏与复杂度

`Q×Q` 在 `Q=128` 时可接受，但正边极少。训练采用：

- 全量正边。
- hard negative：空间接近、沿线有墨迹、节点类别容易混淆的非边。
- 随机负边补足。
- adjacency 使用 focal loss 或正负加权 BCE。

推理可先用距离候选图裁剪不合理 pair，但评测必须确保该裁剪没有降低 edge recall。

---

## 8. 芳香性的双目标

### 8.1 问题

同一规范芳香环可能画成：

- Kekulé 交替单/双键。
- 每边实线加环内虚线伴线。
- 环内圆。
- 在变量环中采用无法唯一 Kekulé 化的伴线画法。

如果两个像素明显不同的图共享一个唯一“画法标签”，模型会收到冲突监督；反之若只学 depicted，又无法得到稳定化学图。

### 8.2 双目标定义

**[约束]** 边同时保存：

```json
{
  "depicted": {
    "stroke_role": "double|single|aromatic_companion|aromatic_circle_member",
    "visible_order": 2
  },
  "normalized": {
    "order": 1,
    "aromatic": true,
    "kekule_order": 2
  }
}
```

说明：

- `depicted` 完全由本张图的像素决定。
- `normalized.aromatic` 表示化学/IR 规范目标。
- `normalized.kekule_order` 仅在存在确定参考 Kekulé 分配时监督，否则为 `null` 并 mask。
- 圆式芳香环可将环级 depiction 记录在 `graph_annotations`，成员边标为 `aromatic_circle_member`。

### 8.3 防止监督冲突

**[约束]** MarkushRender 每次随机化芳香画法时，必须同步产生该次画法的 `depicted` 标签，而 `normalized` 保持不变。不得根据 normalized aromatic 反推出 depicted，也不得把随机画成双线的芳香边标成普通双键。

训练损失分开：

```text
L_edge_semantic = CE(order) + BCE(aromatic)
L_edge_depiction = CE(depiction | adjacency)
```

评测也分别报告 depicted 和 normalized，避免一个指标掩盖另一个问题。

---

## 9. 坐标方案

### 9.1 统一坐标系

**[约束]** 坐标采用：

```text
normalized [0,1], origin top-left, x right, y down
```

所有几何增强后必须同步变换 anchor；裁剪、padding、resize 后重新按最终模型输入归一化。真值来源必须是与最终渲染共享同一 Transform 的真实场景坐标，而不是未渲染 IR 的人工近似坐标。

### 9.2 coarse bin + within-bin offset

**[建议]** 每个轴预测 64-bin 分类与 bin 内偏移：

```python
b = floor(clamp(x, 0, 1-eps) * 64)         # 0..63
o = x * 64 - b - 0.5                       # [-0.5, 0.5)
x_hat = (b + 0.5 + 0.5 * tanh(raw_offset)) / 64
```

或直接将 offset 限制到 `[-0.5, 0.5]`。损失为：

```text
L_coord = CE(x_bin) + CE(y_bin)
        + lambda_off * SmoothL1(x_offset, y_offset)
        + lambda_xy * L1(x_hat, y_hat)
```

相较仅 64-bin，offset 将量化上限从约 `1/128` 图宽的半 bin 误差降为可连续回归；相较纯回归，bin 分类更容易从多峰视觉位置开始学习。

### 9.3 相对键长归一化误差

像素误差不应只按整图宽高报告。评测时估计真值图中可见普通键中心距的中位数 `l_bond_px`，报告：

```text
anchor_error_bond = ||p_pred - p_gt||_2 / l_bond_px
```

对无边或单节点图，同时报告归一化坐标误差，不强行定义键长指标。

---

## 10. 修订后的数据 schema

### 10.1 完整示例

```json
{
  "format": "markush_graph_v2",
  "coordinate_system": "normalized [0,1], origin top-left, y down",
  "image_size": [1024, 768],
  "source": {
    "canonical_structure_id": "mk_000042",
    "scaffold_id": "scaf_0017",
    "patent_family_id": null,
    "render_seed": 23
  },
  "nodes": [
    {
      "id": 0,
      "node_kind": "atom",
      "variable_subtype": null,
      "anchor": {"x": 0.242188, "y": 0.511719, "rule": "bond_vertex"},
      "coord_target": {
        "bins": 64,
        "x_bin": 15,
        "x_offset": 0.0,
        "y_bin": 32,
        "y_offset": 0.25
      },
      "visual_label": {"visible": false, "plain_text": "", "runs": []},
      "semantic_label": {
        "canonical_text": "C",
        "element": "C",
        "h_count": null,
        "charge": 0,
        "isotope": null
      }
    },
    {
      "id": 1,
      "node_kind": "rgroup",
      "variable_subtype": null,
      "anchor": {"x": 0.390625, "y": 0.421875, "rule": "base_core"},
      "coord_target": {
        "bins": 64,
        "x_bin": 25,
        "x_offset": -0.5,
        "y_bin": 27,
        "y_offset": -0.5
      },
      "visual_label": {
        "visible": true,
        "plain_text": "(R7)m",
        "runs": [
          {"text": "(", "script": "base"},
          {"text": "R", "script": "base"},
          {"text": "7", "script": "sub"},
          {"text": ")", "script": "base"},
          {"text": "m", "script": "sub"}
        ]
      },
      "semantic_label": {
        "canonical_text": "(R7)m",
        "family": "R",
        "index": "7",
        "multiplicity": "m"
      }
    },
    {
      "id": 2,
      "node_kind": "ring_placeholder",
      "variable_subtype": null,
      "anchor": {"x": 0.632812, "y": 0.515625, "rule": "placeholder_center"},
      "coord_target": {
        "bins": 64,
        "x_bin": 40,
        "x_offset": 0.0,
        "y_bin": 33,
        "y_offset": -0.5
      },
      "visual_label": {
        "visible": true,
        "plain_text": "A",
        "runs": [{"text": "A", "script": "base"}]
      },
      "semantic_label": {"canonical_text": "A", "placeholder_id": "A"},
      "placeholder": {"shape": "circle"}
    }
  ],
  "edges": [
    {
      "source": 0,
      "target": 1,
      "adjacent": true,
      "normalized": {"order": 1, "aromatic": false, "kekule_order": null},
      "depicted": {"type": "plain", "visible_order": 1},
      "variable": false,
      "wedge_direction": "none"
    },
    {
      "source": 0,
      "target": 2,
      "adjacent": true,
      "normalized": {"order": 1, "aromatic": false, "kekule_order": null},
      "depicted": {"type": "plain", "visible_order": 1},
      "variable": false,
      "wedge_direction": "none"
    }
  ],
  "graph_annotations": [],
  "masks": {
    "cxsmiles": false,
    "normalized_chemistry_complete": true
  }
}
```

### 10.2 schema 不变量

**[约束]** 转换器和测试必须断言：

- 节点 ID 唯一，边端点存在。
- 自环默认非法；确有特殊语义时需新增明确表示，不借用普通边。
- 无向边仅存一次，按节点 ID 规范排序不改变 wedge direction 语义。
- `visual_label.runs` 拼接后等于 `plain_text`。
- script 只允许 `base/sub/sup`。
- 不可见节点的 visual runs 为空，但 semantic label 不为空。
- `ring_placeholder` 必须有 `placeholder_id` 和 `placeholder_center` anchor。
- `variable_subtype` 仅对 variable 节点非空。
- 非邻接 pair 不序列化为 edge。
- 所有不可由源数据确定的字段用 `null/unknown` 并配置 loss mask，不制造伪真值。

### 10.3 与当前 MarkushRender target 的关系

**[事实]** MarkushRender 当前已经实现 `markush_graph_v2` 导出器，而不是让训练加载器零散猜测：

- 从实际渲染布局提取 semantic anchor。
- 同时导出 visual/semantic label。
- 将圆圈占位设为独立、无冲突节点类。
- 边导出 normalized 与 depicted 双目标及方向字段。
- 对无法确定的目标给字段级 mask（例如图片中隐藏的 H 数）。

当前转换器会严格校验 format、坐标、脚本、节点/边类型、楔形方向与圆圈接法；但仍只能说
**预测结构子集可恢复**，不能说 depicted Style、definitions 或完整原始 IR 无损。

---

## 11. Hungarian matching

### 11.1 matching cost

**[建议]** 对 Q 个预测与 N 个 GT 节点构造代价：

```text
C(i,j) =
  lambda_obj   * C_objectness
  + lambda_cls * CE(node_kind_i, kind_j)
  + lambda_xy  * L1(anchor_i, anchor_j)
  + lambda_bin * CE(coord_bin_i, bin_j)
  + lambda_vis * C_visibility
  + lambda_lbl * normalized_edit_cost(label_i, label_j)
```

早期训练 label decoder 尚未稳定时，将 `lambda_lbl=0` 或很小；后期再加入，防止两个邻近、类别相同但标签不同的节点交换。

隐式 C 没有 visual label，主要由坐标、类别和局部拓扑区分。多个对称碳可有多个同等最优匹配，这是正常现象；最终图指标通过同构处理。

### 11.2 拓扑感知 matching

在基础模型稳定后，可加入轻量拓扑迭代：先按节点匹配，再根据 GT 度数与预测 adjacency 估计重算一次 cost。不要在 M1 首版就加入复杂图匹配，以免边头未学会时反过来扰乱节点匹配。

---

## 12. 损失函数

总损失建议：

```text
L = lambda_node * L_node
  + lambda_coord * L_coord
  + lambda_label * L_label_script
  + lambda_attr * L_closed_attributes
  + lambda_adj * L_adjacency
  + lambda_bond * L_bond_semantic
  + lambda_dep * L_depiction
  + lambda_var * L_variable
  + lambda_wedge * L_wedge_direction
  + lambda_aux * L_encoder_aux
  + lambda_cons * L_consistency
```

具体建议：

- `L_node`：objectness focal loss + matched node kind CE；no-object 权重降低。
- `L_coord`：bin CE + within-bin SmoothL1 + decoded coordinate L1。
- `L_label_script`：字符/script token teacher-forced CE，按节点平均，避免长缩写支配 batch。
- `L_closed_attributes`：仅在字段适用且真值已知时计算。
- `L_adjacency`：正边全量 + hard negatives 的 focal/BCE。
- `L_bond_semantic`：order CE + aromatic BCE/CE，条件为 adjacency。
- `L_depiction`：depiction CE，条件为 adjacency 且画法有标注。
- `L_variable`：三值 masked CE 或二元 BCE。
- `L_wedge_direction`：只对 wedge depiction 或明确方向边计算。
- `L_encoder_aux`：可选 bond-junction heatmap、label-center heatmap、line orientation。
- `L_consistency`：边对称性、wedge 反向一致性、visual/semantic parser 一致性。

初始权重不应凭最终指标拍定。先让各项未加权 loss 的梯度量级可观察，再用固定权重或 GradNorm 类策略调平。所有实验记录实际有效样本数，避免大量 mask 后某项 loss 看似很小。

---

## 13. 训练阶段与课程

### 13.1 Stage 0：数据与目标验证

在训练前完成：

- v2 JSON Schema 和 Python dataclass。
- MarkushRender -> v2 转换器。
- schema validator 与可视化 overlay。
- graph -> MarkushRender 回放或最小调试渲染。
- 增强前后 anchor/边/depiction 同步测试。
- 数据切分泄漏检查。

没有可视化抽检通过的数据，不进入正式训练。

### 13.2 Stage 1：普通 OCSR 视觉适配

**[建议]** 从 MolScribe checkpoint 初始化视觉 encoder。新 query decoder、节点头和因子化边头随机初始化。

训练策略：

- 先冻结 encoder 训练新头，类似 MarkushGrapher-2 的 adaptation 原则。
- 将普通分子标注转换为 v2 子集：atom、anchor、label、普通边。
- 普通数据没有 placeholder/variable 时，相应 loss mask，而不是标为负类之外的未知组合。
- 稳定后解冻 encoder 顶部若干 stage，小学习率微调。

这一步建立隐式 C、普通元素、环、键级和坐标能力。

### 13.3 Stage 2：MarkushRender 合成预训练

加入完整 Markush 节点和画法：

- R/M/L 与开放编号。
- `(R)n` 括号和多重度。
- A/B 圆圈、polygon/bracket 占位。
- 缩写、同位素、电荷、H 的前后视觉排列。
- variable/wavy/dotted/wedge。
- 芳香 depicted/normalized 双目标。

逐渐提高困难增强概率，避免模型一开始同时面对模糊、极端旋转和长尾标签。

### 13.4 Stage 3：GT query -> predicted query curriculum

这是避免 MolScribe 式 teacher forcing 落差的关键。

建议三段：

1. **Oracle pair stage**：边头使用 Hungarian 匹配后的 GT-aligned query；坐标可用 GT anchor，学习边语义上限。
2. **Noisy oracle stage**：以递增概率替换为预测 query embedding/anchor，并对 GT anchor 加与真实节点误差相当的噪声；模拟漏检和定位偏差。
3. **Predicted stage**：边头只使用模型预测 positive queries；通过 matching 映射计算真值，加入 false-positive/no-object query，并周期性保留少量 oracle batch 防止崩溃。

可设课程概率：

```text
p_predicted_query: 0.0 -> 0.25 -> 0.5 -> 0.75 -> 1.0
```

每次提升由 validation 的 node recall、anchor error 和 oracle-edge F1 门控，而不是仅按 epoch 固定切换。

### 13.5 Stage 4：混合 replay 防遗忘

**[建议]** Markush 专项训练时持续混入普通 OCSR replay，例如初始 `30%-50%` batch，后续根据普通基准退化调整。

可以分采样器配额：

```text
40% standard OCSR
50% synthetic MarkushRender
10% real Markush warm-up
```

进入真实微调后逐步提高真实比例，但普通 OCSR 不归零。

### 13.6 Stage 5：真实专利数据微调

**[建议]** 真实数据采用小学习率、来源均衡采样和严格 family split。优先修复合成域难以覆盖的因素：扫描噪声、老式字体、压缩、断线、非理想布局、特殊缩写和标注歧义。

训练次序：

- 冻结 encoder 的短 warm-up。
- 解冻高层 encoder。
- 低学习率全模型微调。
- 最后按主指标选择 checkpoint，不以训练 loss 最低作为标准。

### 13.7 MolScribe 初始化映射

可加载：

- 完整 image encoder。
- 尺寸兼容的位置编码，必要时插值。
- 可选复用 Transformer decoder 的 attention/FFN 权重到 NODE AR baseline。

不可直接加载：

- 最终字符词表输出层，除非 token 映射逐项校验。
- 7 类边输出层到因子化头；可只迁移 pair MLP 前层并重新初始化输出层。
- SMILES 位置语义到 set query identity。

加载器必须打印 loaded/missing/unexpected keys 和参数比例，并有单元测试防止“看似加载、实际全随机”。

---

## 14. 数据生成与增强

### 14.1 MarkushRender 作为合成真值源

**[事实]** MarkushRender 的图像与 graph 坐标共享最终 Transform，能提供与画面严格对齐的节点位置；其样式、布局、镜像、键方向和芳香画法由 seed 驱动。

**[建议]** 生成流程：

```text
canonical Markush graph
  -> assign split
  -> sample render/layout/style seed
  -> MarkushRender image + scene-aligned v2 target
  -> optional image-space augmentation + synchronized target transform
  -> schema/geometry QA
  -> shard storage
```

### 14.2 必须先切分，再生成变体

**[约束]** 数据切分顺序不可颠倒：

1. 规范化原始结构，得到 `canonical_structure_id`。
2. 计算 scaffold；真实专利样本同时确定 patent family。
3. 按 canonical graph/scaffold/patent family 分组切分。
4. 每个组只属于一个 split。
5. 在 split 内生成所有样式、布局和清晰度变体。

不得先生成 20 张变体再随机按图片切分，否则同一结构仅换样式就会跨 train/test，形成严重泄漏。

最低切分键优先级：

```text
patent_family_id > canonical_structure_id > scaffold_id > source_record_id
```

评测至少提供 scaffold-disjoint 与 patent-family-disjoint 结果。

### 14.3 增强同步

所有几何增强必须同步作用于：

- node anchors。
- placeholder bbox/radius 辅助目标。
- wedge direction 的几何/构型一致规则。
- depicted line feature 标签。
- 可选字符 bbox。

允许增强：

- 全局旋转、平移、缩放、镜像。
- 轻微透视、扫描倾斜。
- 模糊、JPEG、阈值化、噪点、断墨、低分辨率。
- 字体、字号、线宽、键间距、颜色和画布变化。
- 布局根、支链弯折和合法坐标抖动。
- 芳香画法与圆圈连接画法变化。

约束：

- 镜像必须保持立体化学标注一致，必要时交换楔形语义。
- 图像裁切不得静默删除半个节点；若做 crop 训练，必须定义 partial-object 规则。
- 图像空间增强不能把 base/sub/sup 标签改成错误语义。
- 每个变体保存 seed 和增强参数，确保可复现。

### 14.4 数据 QA

每批生成后自动检查：

- schema 合法。
- 节点数、边数在容量内。
- anchor 位于画布内。
- visual label 与实际渲染 label script 一致。
- 无 dangling edge。
- wedge 方向在增强后保持。
- depicted/normalized 芳香标签一致但不混淆。
- 图像 hash 和结构 ID 不跨 split。

随机抽样绘制 overlay：anchor 十字、节点 ID、label runs、预测边中线和楔方向。数据问题优先于模型调参修复。

---

## 15. 训练伪代码与张量形状

### 15.1 Set-based 主路线

设：

```text
B: batch size
H,W: 输入高宽
D: hidden size，建议 256
Q: node queries，建议初始 128
T: 每节点最大 label token 数，建议初始 32
F: encoder feature tokens
K: 沿线采样点数，建议 16
```

主要张量：

```python
images             # [B, 3, H, W]
memory             # [B, F, D]
queries            # [B, Q, D]
object_logits      # [B, Q, 2]
kind_logits        # [B, Q, 5]
x_bin_logits       # [B, Q, 64]
y_bin_logits       # [B, Q, 64]
offsets            # [B, Q, 2]
label_logits       # [B, Q_pos, T, V_label]
line_features      # [B, Q, Q, K, D_line] -> pooled [B,Q,Q,D_line]
adj_logits         # [B, Q, Q]
order_logits       # [B, Q, Q, 5]
aromatic_logits    # [B, Q, Q, 3]
depiction_logits   # [B, Q, Q, C_dep]
variable_logits    # [B, Q, Q, 3]
wedge_dir_logits   # [B, Q, Q, 4]
```

伪代码：

```python
memory, pyramid = encoder(images)
query_states = node_decoder(learned_queries, memory)

node_pred = node_heads(query_states)
matches = hungarian_match(node_pred.detach_for_matching(), targets)

roi_features = gather_label_roi(pyramid, node_pred.anchors, matches)
label_pred = label_decoder(query_states, roi_features, teacher_tokens=targets.label_tokens)

edge_queries, edge_target_map = curriculum_select_queries(
    query_states=query_states,
    predictions=node_pred,
    matches=matches,
    gt_nodes=targets.nodes,
    predicted_probability=schedule.value,
)
line_features = sample_along_candidate_lines(pyramid, edge_queries.anchors)
edge_pred = edge_heads(edge_queries.states, edge_queries.anchors, line_features)

loss = compute_node_losses(node_pred, label_pred, matches, targets)
loss += compute_factorized_edge_losses(edge_pred, edge_target_map, targets)
loss.backward()
optimizer.step()
```

### 15.2 显式 NODE AR baseline

```python
memory = encoder(images)                              # [B,F,D]
token_hidden = ar_decoder(gt_tokens[:, :-1], memory) # [B,L-1,D]
token_logits = token_head(token_hidden)              # [B,L-1,V]
node_hidden = gather_at_node_end(token_hidden, gt_node_indices)  # [B,N,D]
edge_pred = factorized_edge_heads(node_hidden, gt_or_pred_coords)
```

训练中必须逐步将 `gt_node_indices` 替换为预测序列解析出的节点位置，否则会重复 MolScribe 的 teacher forcing 落差。

### 15.3 容量与 mask

- `Q` 是最大预测节点数；超出应显式报 `capacity_exceeded`，不可截断后仍当正常样本。
- `T` 是单节点 label 最大 token 数，超长缩写单独统计并考虑 byte/character fallback。
- pair tensor 只对 positive/candidate query 计算完整头以节省显存。
- 所有 unknown/null 字段必须对应 loss mask。

---

## 16. 推理与图组装

### 16.1 推理步骤

```text
1. 图像预处理并保留 inverse transform。
2. 预测 Q 个节点 query。
3. 过滤 no-object，保留节点置信度与容量警告。
4. 解码 visual label script 和闭集属性。
5. 构造 semantic label；冲突写 warning。
6. 对候选节点对预测因子化边。
7. 应用对称性、方向性和阈值约束。
8. 在模型输入坐标与原图坐标之间反变换 anchor。
9. 输出原始图；可选运行非破坏性化学校验和 CXSMILES 导出。
```

### 16.2 后处理边界

允许的确定性后处理：

- 去除 self-edge。
- 对称化无向属性。
- wedge direction 反向映射。
- 将 label script 解析为 element/R index/multiplicity 等字段。
- 标记而非删除违反常见价态的边。

默认不允许：

- 为满足价态自动改键级且覆盖模型原始预测。
- 因 CXSMILES 无法表示而删除特殊节点。
- 用最近邻强连所有孤立节点。
- 将 A/B placeholder 当成普通 B 元素。

---

## 17. CXSMILES 的角色

### 17.1 仅为辅助和导出

**[约束]** CXSMILES 不能覆盖或替代主图。主图是项目事实源；CXSMILES 是：

- 普通骨架和可表达 Markush 特征的兼容导出。
- 可选辅助 decoder 的训练目标。
- 与已有 OCSR/MMSR 系统比较的次级指标。
- 化学有效性和规范化的一种检查途径。

### 17.2 无法表达时如何处理

若 CXSMILES 工具链不能无损表达某个 placeholder、特殊变量节点、画法属性或开放标签：

- 主图完整保留。
- `masks.cxsmiles=false` 或对不可表达片段做 span-level mask。
- `auxiliary.cxsmiles=null` 并记录 `cxsmiles_unrepresentable:<reason>`。
- 不把特殊节点改写成错误的普通原子以换取字符串合法。

任何 graph -> CXSMILES -> graph round-trip 测试只适用于“声明可表达”的子集，不能作为全 schema 无损证明。

---

## 18. 评测方案

### 18.1 主指标：图同构 exact match

**[约束]** 在忽略节点 ID 和预测 query 顺序后比较图同构。节点颜色/属性至少包括：

- `node_kind`、`variable_subtype`。
- semantic rich label。
- placeholder identity/shape（按具体评测层级）。
- 可见性。

边颜色/属性包括 normalized order/aromatic、variable、wedge direction；depicted 属性另设 exact 指标。

报告：

```text
Graph Exact / Semantic
Graph Exact / Semantic + Depicted
Graph Exact / Rich Label Strict
```

### 18.2 空间匹配

节点级指标先通过 Hungarian matching 或带属性的最优二分匹配建立对应，再报告：

- Node precision/recall/F1。
- Node kind accuracy。
- semantic anchor mean/median/P90 error。
- 相对键长误差。
- 阈值内召回：`<=0.1`, `<=0.25`, `<=0.5` bond length。

空间匹配不能只靠坐标；近邻 R1/R2 必须结合类别和标签，防止错误交换被算作正确。

### 18.3 rich-label 指标

分别报告：

- exact visual label script。
- plain-text exact。
- script-aware normalized edit distance。
- base/sub/sup token F1。
- semantic label exact。
- R/M/L family accuracy 与 index exact。
- multiplicity exact。
- charge/isotope/H count accuracy。
- abbreviation exact 与 character error rate。

`R1` 与 `R¹` plain text 相同但 script 不同，strict rich-label 必须判错。

### 18.4 placeholder 指标

- placeholder node precision/recall/F1。
- A/B identity accuracy。
- shape accuracy。
- center error / bond length。
- incident-edge exact 与 F1。

### 18.5 边指标：oracle 与 end-to-end 分开

**Oracle edge F1**：使用 GT 节点及 GT 对齐/GT anchor，只评边头识别能力。

**End-to-end edge F1**：先匹配预测节点与 GT 节点，再评预测边；漏节点造成的边漏检必须计入。

每种都报告：

- adjacency F1。
- bond order macro/micro F1。
- aromatic F1。
- depiction macro F1。
- variable edge F1。
- wedge presence 与 wedge direction accuracy。

两者差距直接量化 predicted-query 落差。

### 18.6 芳香双指标

- `Normalized Aromatic Edge F1`。
- `Normalized Bond Exact`。
- `Depicted Aromatic Style F1`。
- `Depicted Edge Exact`。
- 环级 aromatic exact。

### 18.7 CXSMILES 辅助指标

仅在 `masks.cxsmiles=true` 子集报告：

- parse validity。
- canonical CXSMILES exact。
- 去 Markush 特征后的骨架 InChIKey/graph equivalence。
- graph -> CXSMILES -> graph 的可表达字段保持率。

CXSMILES 指标不得替代 Graph Exact。

### 18.8 数据分层

所有主指标至少按以下切片报告：

- 合成 vs 真实。
- patent office/年份/分辨率。
- 节点数和边数区间。
- 是否含 placeholder、R/M/L、缩写、多重度、楔形、芳香。
- scaffold seen/unseen。
- patent family seen/unseen，正式 test 必须 unseen。
- 清晰/模糊/低分辨率。

---

## 19. 模块目录

```text
MarkushScribe/
├── 方案.md
├── pyproject.toml
├── configs/
│   ├── model_set.yaml
│   ├── model_node_ar.yaml
│   ├── train_synthetic.yaml
│   └── finetune_real.yaml
├── markushscribe/
│   ├── __init__.py
│   ├── interface.py                 # predict_image / batch API
│   ├── schema.py                    # markush_graph_v2 dataclass + validation
│   ├── transforms.py                # image/anchor synchronized transforms
│   ├── tokenizer.py                 # label-script tokenizer
│   ├── dataset.py                   # shard loading and collate
│   ├── split.py                     # canonical/scaffold/family split
│   ├── model/
│   │   ├── encoder.py
│   │   ├── node_decoder.py          # set query decoder
│   │   ├── node_ar.py               # explicit NODE AR baseline
│   │   ├── label_decoder.py
│   │   ├── line_sampler.py
│   │   ├── edge_heads.py
│   │   └── model.py
│   ├── matching/
│   │   ├── hungarian.py
│   │   └── costs.py
│   ├── losses/
│   │   ├── node.py
│   │   ├── label.py
│   │   └── edge.py
│   ├── training/
│   │   ├── trainer.py
│   │   ├── curriculum.py
│   │   └── checkpoint.py
│   ├── inference/
│   │   ├── decode.py
│   │   ├── assemble.py
│   │   └── confidence.py
│   ├── evaluation/
│   │   ├── isomorphism.py
│   │   ├── spatial.py
│   │   ├── labels.py
│   │   └── edges.py
│   ├── chemistry/
│   │   ├── validate.py
│   │   ├── rdkit_export.py
│   │   └── cxsmiles.py
│   └── visualization/
│       └── overlay.py
├── tools/
│   ├── build_render_dataset.py
│   ├── validate_dataset.py
│   ├── check_split_leakage.py
│   └── inspect_predictions.py
├── tests/
│   ├── test_schema.py
│   ├── test_transforms.py
│   ├── test_matching.py
│   ├── test_label_script.py
│   ├── test_edge_symmetry.py
│   ├── test_aromatic_targets.py
│   ├── test_split_leakage.py
│   └── test_debug_train.py
└── MolScribe/                      # 只读参考实现
```

模块边界要求：

- `schema.py` 不依赖模型。
- `evaluation` 只消费 v2 schema，不依赖 decoder 内部张量。
- `chemistry` 导出失败不能破坏主预测。
- `MolScribe/` 保持只读；复用通过加载 checkpoint 或复制必要思想，不在其目录开发。

---

## 20. 里程碑 M0-M5

### M0：契约与数据闭环

交付：

- `markush_graph_v2` schema、validator 和示例。
- MarkushRender v2 target 适配设计与离线转换器。
- visual/semantic label parser。
- split 工具和泄漏检查。
- overlay 查看器。
- 至少 100 个样本人工抽检记录。

退出条件：schema 单测全绿；增强后 anchor/edge 同步；没有跨 split 的 canonical/scaffold/family 冲突。

### M1：显式 NODE AR baseline

交付：

- MolScribe encoder 初始化。
- NODE AR + label script + factorized edge heads。
- 普通 OCSR 和合成 Markush 小规模训练。
- oracle/end-to-end 分解评测。

退出条件：单 batch 过拟合；debug 训练可复现；比“SMILES 直接扩词表”基线在 rich-label/placeholder 上明显更好。

### M2：Set-based 主模型

交付：

- learned queries、Hungarian matching。
- node/anchor/label 多头。
- 相对坐标和沿线视觉特征边头。
- 与 NODE AR 公平消融。

退出条件：set 模型在相同数据和 encoder 下达到更高 Graph Exact 或相同精度下更低长结构失败率。

### M3：课程训练与完整合成域

交付：

- GT query -> predicted query curriculum。
- 芳香双目标、variable、wedge direction。
- 全 MarkushRender 样式/布局增强。
- 普通 OCSR replay。

退出条件：end-to-end edge F1 与 oracle edge F1 的差距显著收窄；普通 OCSR 图 exact 无不可接受退化。

### M4：真实专利微调

交付：

- family-disjoint 真实训练/验证/测试集。
- 来源均衡微调。
- 错误分析和置信度校准。
- 可选 CXSMILES 导出。

退出条件：真实集主指标达到本文验收门槛，且合成到真实的差距有明确分层解释。

### M5：生产化与扩展准备

交付：

- 稳定批量 API、模型卡、可复现实验配置。
- 性能/显存/延迟基准。
- 数据和 checkpoint 版本管理。
- 为未来 OCR/VTL 定义不破坏 v2 主图的扩展接口。

退出条件：从图片到 v2 JSON 的完整流程可部署、可审计、可回归；不依赖定义表服务也能独立输出主图。

---

## 21. 风险与应对

| 风险 | 表现 | 应对 |
| --- | --- | --- |
| 合成到真实域差距 | 合成高分、老专利低分 | 多样渲染、真实微调、按来源分层 |
| 隐式 C 漏检 | 节点/边同时缺失 | junction 辅助头、高分辨率特征、纯烃 replay |
| 上下标太小 | R1/R2 混淆、多重度漏识别 | ROI label decoder、512 输入消融、script loss |
| query 容量不足 | 大图静默截断 | P99 定容、容量告警、动态 query/裁块 |
| 对称节点 matching 抖动 | loss 波动、边身份交换 | 图同构评测、后期拓扑 cost、稳定坐标 cost |
| teacher forcing 落差 | oracle edge 高、E2E 低 | predicted-query curriculum |
| 单一大边类稀疏 | variable+wedge 组合学不会 | 因子化边头与 masked loss |
| 芳香监督冲突 | 同像素/同化学目标互相矛盾 | depicted/normalized 双目标 |
| 缩写长尾 | `<unk>` 多 | 字符级开放 decoder、真实词表统计 |
| A/B 与元素 B 混淆 | 占位环被当硼原子 | 独立 placeholder 类 + 轮廓局部特征 |
| 化学后处理掩盖错误 | 指标虚高、无法审计 | 保存 raw prediction，主指标先评 raw graph |
| CXSMILES 表达不全 | 特殊节点丢失 | 主图优先、不可表达 mask |
| split 泄漏 | 测试虚高 | 先分组切分再生成变体，hash 审计 |
| 普通 OCSR 遗忘 | Markush 变好、普通分子变差 | replay 与双域 checkpoint 选择 |
| 标签真值不一致 | anchor/label loss 有噪声 | v2 converter 单源、overlay 抽检、schema 版本化 |

---

## 22. 消融计划

所有消融固定数据切分、encoder 初始化、训练步数和随机种子，至少报告 3 个 seed 的均值与标准差。

### 22.1 架构消融

- NODE AR vs set-based queries。
- 纯最终层 feature vs 多尺度 feature。
- 无 ROI label feature vs ROI label decoder。
- `Q=64/128/192` 的容量与显存权衡。

### 22.2 matching 与坐标

- 仅坐标 matching vs 类别+坐标 vs 加 label cost。
- 64-bin only vs continuous only vs bin+offset。
- 图宽归一误差 vs 相对键长误差作为选择依据。

### 22.3 边头

- MolScribe 式 7 类联合头 vs 因子化边头。
- 仅 query pair vs +相对坐标 vs +沿线视觉特征。
- 全负样本 vs hard-negative sampling。
- 无 curriculum vs noisy oracle vs 完整 predicted-query curriculum。

### 22.4 标签

- 固定闭集标签 vs 字符 script decoder。
- 单一 label vs visual/semantic 双标签。
- plain text vs base/sub/sup script。
- 无闭集属性辅助头 vs 有辅助头。

### 22.5 数据与迁移

- 随机初始化 vs MolScribe encoder 初始化。
- 合成 only vs 合成+真实。
- Markush only vs +普通 OCSR replay。
- 单一渲染样式 vs StyleSampler vs Style+LayoutSampler。
- 芳香单目标 vs depicted/normalized 双目标。

### 22.6 评判原则

不能只看 CXSMILES exact 或 token accuracy。架构选择按以下优先级：

```text
真实集 Graph Exact
> end-to-end edge F1
> rich-label exact / placeholder F1
> anchor error
> oracle edge F1
> CXSMILES auxiliary
```

---

## 23. 验收标准

以下是进入生产候选前的建议门槛；M0 时应根据真实集规模锁定最终数值，锁定后不得根据 test 结果反复调低。

### 23.1 功能验收

- 输入单张 RGB 结构图可输出合法 `markush_graph_v2`。
- 支持空图、单节点、无可见标签纯碳图、多环、大图。
- 支持 R1/R2、M/L、缩写、base/sub/sup、括号、多重度和 A/B 占位环。
- 输出因子化边、wedge direction 和芳香双目标。
- CXSMILES 失败时主图仍完整返回。
- 不输出或猜测 `variable_def/rgroup_definitions`。

### 23.2 数据验收

- 100% 样本通过 schema validator。
- 100% 边端点有效，节点 ID 唯一。
- 几何增强同步测试全绿。
- canonical structure、scaffold、patent family 无跨 split 泄漏。
- 随机抽检至少 500 个合成变体、每个真实来源至少 100 张 overlay；关键字段错误率低于 0.5%，发现系统性错误必须返工数据而非交给模型容忍。

### 23.3 模型建议门槛

在锁定的 family/scaffold-disjoint 真实测试集上：

- Node F1 >= 0.97。
- Rich semantic label exact >= 0.95。
- Placeholder F1 >= 0.95，A/B identity accuracy >= 0.98。
- Adjacency end-to-end F1 >= 0.96。
- 因子化 bond semantic macro F1 >= 0.94。
- Wedge direction accuracy >= 0.95（仅在有方向真值样本上）。
- Anchor median error <= 0.15 个键长，P90 <= 0.35 个键长。
- Semantic Graph Exact >= 0.80；同时报告按节点规模切片，不允许只报总体。
- Oracle 与 end-to-end adjacency F1 差距 <= 3 个百分点。
- 相比无 replay 方案，普通 OCSR Graph Exact 的退化不超过 2 个百分点。

这些门槛是**设计建议而非已证实性能**，必须在数据集规模与标注一致性明确后冻结。

### 23.4 工程验收

- 固定 seed 的数据生成和推理可复现。
- checkpoint 明确记录 schema、词表、配置、git revision 和数据版本。
- CPU 可运行一个 debug batch；目标硬件可运行完整验证集。
- 单元测试覆盖 schema、坐标变换、matching、label parser、边对称性、芳香双目标、split 泄漏和 checkpoint 加载。
- 任何容量截断、CXSMILES 不可表达、语义冲突均进入 warnings/统计，不静默发生。

---

## 24. 实施优先级与决策记录

### 24.1 已定约束

1. 主任务是纯结构图到显式图，不预测变量定义表。
2. 主输出是 `markush_graph_v2`，不是 SMILES/CXSMILES。
3. 最终优先 set-based queries + Hungarian matching。
4. 可先实现显式 NODE AR baseline，但不把它视为最终默认方案。
5. 节点同时保存 visual label 与 semantic label。
6. 坐标是 semantic anchor，采用 64-bin + within-bin offset。
7. 边使用因子化头，并加入相对坐标与沿线视觉特征。
8. 芳香使用 depicted/normalized 双目标。
9. 训练必须包含 GT query -> predicted query curriculum。
10. 先按结构/scaffold/patent family 切分，再生成增强变体。
11. CXSMILES 只辅助，不覆盖主图；不可表达部分必须 mask。
12. 不宣称当前 `target_to_ir` 对完整目标无损。

### 24.2 需要实验决定

- 输入 384 还是 512，以及是否需要 ROI label encoder。
- `Q`、`T`、D 和沿线采样 K 的最终值。
- set 模型是否需要拓扑感知二次 matching。
- encoder 解冻层数和普通 OCSR replay 比例。
- closed attribute heads 的实际增益。
- 真实数据达到何种规模后停止扩大合成比例。
- CXSMILES 辅助 decoder 是否改善主图，还是只增加负迁移。

### 24.3 将来扩展

当项目扩展到整页 Markush 多模态识别时，可借鉴 MarkushGrapher-2 增加：

```text
structure image encoder
+ ChemicalOCR
+ VTL encoder
+ definition/table decoder
```

但扩展应通过 `document_annotations` 或独立定义服务引用主图节点 ID；不得修改主图中已经稳定的节点/边含义，也不得让定义表识别成为纯结构图推理的前置条件。

---

## 25. 事实来源与自检清单

### 25.1 已核对来源

- MarkushScribe `AGENTS.md`：项目定位、MolScribe 只读约束与原始 IR 方向。
- MarkushRender `DESIGN.md`：渲染到训练目标的坐标一致性、富标签、占位环、芳香画法和增强约束。
- MarkushRender `markushrender/target.py`：当前 v2 的 visual/semantic 双标签、anchor + bin/offset、normalized/depicted 边、字段级 mask 与定义字段丢弃行为。
- MolScribe `molscribe/tokenizer.py`：`chartok_coords` 中原子字符后追加 x/y 坐标 token。
- MolScribe `molscribe/model.py`：teacher forcing 路径、`atom_indices` 和 `N×N×7` GraphPredictor。
- MolScribe `molscribe/utils.py`：`chartok_coords.max_len=480`，其单位为解码 token。
- MolScribe `molscribe/interface.py`：发布接口默认 `coord_bins=64` 和 7 类边名称。
- MarkushGrapher-2 论文（arXiv:2603.28550）：双编码器、ChemicalOCR、VTL、两阶段训练及数据规模。

### 25.2 文档自检

- 已覆盖项目目标、非目标和输入输出契约。
- 已准确区分 MolScribe 的 token 序列上限与节点数。
- 已说明 MolScribe teacher forcing 的训练/推理落差。
- 已限定 MarkushGrapher-2 对本任务的适用边界，没有把定义表 OCR 设为首期要求。
- 已给出 set-based 主路线与 NODE AR baseline。
- 已定义 visual/semantic label、semantic anchor、无冲突节点 schema、隐式 C 与圆圈节点。
- 已定义开放 label-script decoder 与闭集属性头的分工。
- 已定义因子化边头、相对坐标和沿线视觉特征。
- 已定义芳香 depicted/normalized 双目标。
- 已定义 64-bin + within-bin offset 坐标。
- 已给出训练阶段、损失、query curriculum、普通 OCSR replay、MolScribe 初始化和真实微调。
- 已规定先切分后生成变体，覆盖 scaffold/patent family 泄漏。
- 已覆盖图同构、空间、rich-label、placeholder、oracle/end-to-end edge、坐标、芳香和 CXSMILES 评测。
- 已明确 CXSMILES 只辅助且不可表达字段要 mask。
- 已给出模块目录、schema 示例、张量形状、训练伪代码、M0-M5、风险、消融和验收标准。
- 已明确区分事实、建议与约束。
- 已明确不宣称当前 `target_to_ir` 对完整 Markush IR 或 Style 级 depiction 无损，只恢复可映射的预测结构子集。
